<div class="columns is-centered mt-6">
  <div class="column is-narrow">
    <h1 class="title is-size-3 has-text-centered">Notas Fiscais de Serviço Eletronica</h1>
    <div class="buttons is-centered">
      <%= link_to "Voltar para Clientes", customers_path, class: "button is-link is-small" %>
    </div>
    <table class="table">
      <thead>
        <tr>
          <th>Data</th>
          <th>Cliente</th>
          <th>Estado</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @bills.each do |bill| %>
          <%= render bill %>
        <% end %>
      </tbody>
    </table>
    <%= render 'shared/pagination', pagination: @pagination %>
  </div>
</div>
