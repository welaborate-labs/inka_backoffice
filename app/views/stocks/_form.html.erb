<%= form_with model: [@product, @stock] do |f| %>
  <%= render 'shared/error_messages', resource: @stock %>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.text_field :quantity %>
  </div>

  <div class="field">
    <%= f.label :integralized_at %>
    <%= f.date_select :integralized_at %>
  </div>

  <div>
    <%= f.label :entrada_ou_saida, class: "display: block" %>
    <%= f.select :type, options_for_select([['entrada', 'StockIncrement'], ['saida', 'StockDecrement']]) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
